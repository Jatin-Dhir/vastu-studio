# Copy to .env.local (gitignored). Both values come from the Supabase project:
# Project settings → API. The anon key is safe to ship — row-level security does the guarding.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
